# Handoff prompt — paste this into a fresh session

Copy everything below the line into a new Claude Code session started in
`~/Projects/aeo-tracker`.

---

Продолжаем работу над фичей **Run Comparison** для `aeo-platform` (репозиторий
`~/Projects/aeo-tracker`, v1.7.4). Предыдущая сессия закончилась по контексту, вся
подготовка сохранена на диск.

**Сначала прочитай, в этом порядке:**

1. `docs/run-comparison/00-STATUS.md` — что сделано, что дальше, какие решения приняты и почему
2. `docs/run-comparison/01-report-model-and-uvi.md` — модель отчёта, загрузка снапшотов, API `visibility-index`, форма ячейки результата, существующий `lib/diff.js`, регистрация markdown-секций, конвенции тестов (всё с номерами строк)
3. `docs/run-comparison/02-html-render-pipeline.md` — `renderHtml`, скелет документа, композиция секций, `esc()`, готовая механика тогглов, поверхности white-label, дизайн-токены, print-блок, хелперы графиков
4. `CODING_STANDARDS.md` — обязательные конвенции (ESM, ноль зависимостей, ≤300 строк на файл, ≤50 на функцию, JSDoc на экспортах, `esc()` на интерполяциях, никаких бэктиков в комментариях внутри template literals)

**Что уже готово и работает** (в рабочем дереве, не закоммичено, 23/23 теста зелёные):

- `lib/report/comparison-segments.js` — сегментация ячеек на lost / held / gained / never / indeterminate
- `lib/report/comparison-drivers.js` — причинное разложение движения индекса на like-for-like и композиционное
- `test/comparison-segments.test.js`, `test/comparison-drivers.test.js`
- `package.json` — оба модуля в `test:imports`, оба теста в цепочке `npm test`

Проверь, что всё ещё зелено: `npm run test:comparison-segments && npm run test:comparison-drivers`

**Следующий шаг — `lib/report/run-comparison.js` (оркестратор).** Точные требования к нему
перечислены в `00-STATUS.md`, раздел «Next steps», пункт 1. Кратко: собрать полную модель
сравнения из массива снапшотов, выбирая пару **относительно рендеримого снапшота** (не
новейшего на диске — `--for-date` обрезает массив на месте), классифицировать компоненты
direct/conditional по `sample.basis`, учесть смену базы весов при `null`-оси, подключить
подавление шума через `classifyProportionChange` для прогонов с `--samples N`, и посчитать
замещение конкурентов по потерянным ячейкам.

Дальше: HTML-секция (верхнеуровневый блок в едином скролле, **не вкладки** — обоснование в
`00-STATUS.md`), затем markdown-секция (регистрировать в **трёх** списках в
`lib/report/markdown.js`, иначе пропадёт из white-label).

**Перед тем как объявить готово:** отрендерить реальный отчёт с `--white-label` и прогнать
grep на утечки по **отрендеренному HTML**, а не по исходникам:

```
grep -Eaic 'aeo-platform|@?webappski|mission control|mc-bridge|lib/[a-z]|session.?cost|generated by [a-z]|outreach|guest post|push for' <файл>
```

Должен быть 0.

**Данные для проверки на реальном прогоне** лежат в другом проекте —
`~/Projects/autopro-service/aeo-responses/2026-06-17/_summary.json` и `.../2026-08-24/_summary.json`
(бренд Gcore, замороженная корзина из 6 запросов × 3 движка = 18 ячеек на прогон). На этой паре:
сегменты 4 lost / 1 held / 2 gained / 11 never, UVI 45 → 31, и ключевой факт —
like-for-like дельта тона ровно 0.0 (падение Sentiment целиком объясняется одним новым
нейтральным упоминанием Claude, то есть приобретением). Это зафиксировано регрессионным тестом.

**Дизайн-макет от дизайнера** (использовать как визуальный ориентир, но с тремя оговорками,
описанными в `00-STATUS.md`, раздел «Decisions taken that deviate from the designer's handoff»):
`~/Downloads/design_handoff_run_comparison/` — там `Run Comparison.dc.html` (интерактивный
прототип), `README.md` (подробный хендофф с токенами и копирайтом) и файлы данных.

Важно: инлайновые стили и сырые hex из макета переносить нельзя — `test/design-lint.test.js`
жёстко падает на инлайновом `font-size` в `html.js`, а в системе есть токены ровно под эти роли:
lost → `--bad`, held/gained → `--good`, never-present → `--editor`, предупреждения → `--warn`.

**Тон отрендеренного результата:** это клиентский документ, который читает нетехнический
руководитель. Никакого инженерного языка в выводе — ни «fixed», ни «bug», ни «pipeline»,
ни «extractor», ни названий моделей. Оговорки о методологии — простым языком и во
второстепенном месте, никогда не бейджем рядом с заголовочными цифрами.

Ничего не коммить без явной просьбы — ветка `master`.
