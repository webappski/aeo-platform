# Handoff prompt v2 — paste this into a fresh session

Copy everything below the line into a new Claude Code session started in
`~/Projects/aeo-tracker`.

---

Продолжаем **Run Comparison** для `aeo-platform` (`~/Projects/aeo-tracker`, v1.7.4).
Предыдущая сессия закончилась не по контексту, а по содержанию: фича была
дорендерена, все тесты зелёные, данные проверены на реальном прогоне Gcore — но
результат забраковали по делу. Вся причина и план записаны на диске, читай в этом
порядке:

1. `docs/run-comparison/03-visual-prominence-pivot.md` — **начни отсюда**, это
   разбор проблемы и план. Коротко: секция технически верна, но визуально
   незаметна («ну и хрен с ним, ничего страшного»), хотя должна «кричать» —
   неподготовленный менеджер должен за 3 секунды понять, где теряются позиции.
   Плюс: сравнение сейчас считается только между двумя последними прогонами,
   а нужно думать в терминах N прогонов — динамика к прошлому разу И к самому
   первому прогону, а не статичная пара чисел.
2. `~/Downloads/design_handoff_run_comparison/README.md` — оригинальный
   дизайн-хендофф (табы, comparison как primary-контент, точные цвета/типографика/
   копирайт). Именно это решение по табам предыдущая сессия сознательно
   переопределила («No tabs», см. `00-STATUS.md`) — пересмотри это решение заново,
   не наследуй его молча.
3. `~/Downloads/design_handoff_run_comparison/aeo-platform-comparison-design-brief.md`
   — исходный продуктовый бриф с формулировкой «analytical job» и явным
   требованием «Design for N runs, not 2».
4. `00-STATUS.md` — что уже реализовано и работает (переиспользовать, не
   переписывать с нуля): `lib/report/comparison-segments.js`,
   `lib/report/comparison-drivers.js`, `lib/report/run-comparison.js` — 42
   тестов зелёные, сверено с реальными данными Gcore.

**Первым делом**, до любого кода: погугли/проверь best practices по UX для
period-over-period / trend-репортинга в аналитических продуктах — этого
намеренно не делали в прошлой сессии (экономили контекст на запись хендоффа).
Дальше — реши вопрос размещения (табы назад vs. другой способ дать секции вес
героя) с обоснованием, распиши это решение письменно, только потом код.

**Жёсткие требования, зафиксированные основателем:**
- Секция сравнения должна работать **на всех отчётах** (не только
  `--white-label`), **без настроек/флагов** — включается сама, как только у
  домена есть 2+ прогона.
- Оригинальный макет дизайнера должен быть буквально доступен как референс —
  вставлен ссылкой (верх/низ отчёта) один-в-один, а не переписан по памяти.
- Тон/жаргон-контракт (никаких «cell», «extractor», «pipeline», «bug», «fixed»,
  названий моделей) — уже реализован верно в существующем коде, сохранить.
- Полный редизайн всей AEO Platform под дизайн-систему Webappski — это
  ОТДЕЛЬНАЯ, более поздняя инициатива. В этой сессии — только Run Comparison.

Ничего не коммить без явной просьбы — ветка `master`.
